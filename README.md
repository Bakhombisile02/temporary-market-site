## Rppl Marketing Site 


### Develop

1. Install packages 
`npm i`

`cd site && bundle`

2. Run (live reload)

`npm start`

3. Build (if need to)

`npm run build`

### Deploy

`npm run build`
`firebase deploy --only hosting`
